<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edits</title>
</head>
<style>
    body {
        margin: 0;
        padding: 0;
    }

    .main-container {
        display: flex;
        justify-content: flex-start;
        align-items: center;
        width: 100%;
        min-height: 100vh;
        flex-direction: column;
        background-image: linear-gradient(to top, #a29bfe 0%, #330867 100%);
        padding: 50px 20px;
    }

    .main-container>.main-heading {
        color: #fdcb6e;
        font-size: 1.5rem;
    }

    .main-container>.main-content {
        padding: 20px;
        background: rgba(255, 255, 255, 0.2);
        -webkit-backdrop-filter: blur(5px);
        backdrop-filter: blur(5px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        margin: 50px 0;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: row;
        min-width: 500px;
    }

    .btn-grad {
        background-image: linear-gradient(to left, #1D2B64 0%, #6c5ce7 51%, #1D2B64 100%);
        margin: 10px 0;
        margin-right: 10px;
        padding: 10px 20px;
        text-align: center;
        text-transform: uppercase;
        transition: 0.5s;
        background-size: 200% auto;
        color: white;
        display: block;
        outline: none;
        border: none;
    }

    .btn-grad:hover {
        background-position: right center;
        /* change the direction of the change here */
        color: #fff;
        text-decoration: none;
    }

    .btn-grad-1 {
        background-image: linear-gradient(to right, #485563 0%, #29323c 51%, #485563 100%);
        margin: 10px 0;
        padding: 10px 20px;
        text-align: center;
        text-transform: uppercase;
        transition: 0.5s;
        background-size: 200% auto;
        color: white;
        display: block;
        border: none;
    }

    .btn-grad-1:hover {
        background-position: right center;
        /* change the direction of the change here */
        color: #fff;
        text-decoration: none;
    }

    .custom-input {
        padding: 10px 20px;
        outline: none;
        background: rgba(255, 255, 255, 0.05);
        -webkit-backdrop-filter: blur(2px);
        backdrop-filter: blur(2px);
        border: 1px solid rgba(255, 255, 255, 0.025);
        border: none;
        color: #2d3436;
        width: 500px;
        border: 1px dashed black;
    }

    .custom-input:focus {
        background: rgba(255, 255, 255, 0.264);
        -webkit-backdrop-filter: blur(2px);
        backdrop-filter: blur(2px);
        border: 1px solid rgba(255, 255, 255, 0.025);
    }

    .main-content>p {
        width: 500px;
    }

    @media screen and (max-width:799px) {
        .custom-input {
            width: 300px;
        }

        .main-container>.main-content {
            min-width: 300px;
        }
    }

    .custom-input::placeholder {
        color: #2d3436;
    }
</style>
<body>
    <div class="main-container">
        <div class="main-heading">
            Spell Correction for Dyslexic Peoples
        </div>
        <div class="main-content">
            <form action="/edits" method="post">
                <div class="mb-3">
                    <textarea class="custom-input" id="input-text" name="question" placeholder="type here"
                        rows="10"></textarea>
                </div>
                <div style=" display: flex;flex-wrap:nowrap;justify-content: center;">
                    <button type="submit" class="btn-grad">Find</button>
                    <button type="reset" class="btn-grad-1">Cancel</button>
                </div>
            </form>
        </div>
    </div>
</body>

</html>